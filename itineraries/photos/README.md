Photo binaries are not kept in git.

Use an HTTPS `photo:` URL in the itinerary YAML (Wikimedia Commons thumbs work well).
See `CREDITS.md` for attributions (New Zealand + Netherlands).

Optional helper: `scripts/fetch_nz_photos.py` (local download for offline experiments).
